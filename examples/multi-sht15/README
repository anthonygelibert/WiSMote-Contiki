To use this example, you must replace in "contiki-main.c" the line:

SENSORS(...); 

where "..." matches the current list of sensors, by :

#include "multsht15-sensors.h"
SENSORS(..., &upper_sensor, &lower_sensor);
 